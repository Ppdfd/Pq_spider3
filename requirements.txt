# PQ-SPIDER runtime dependencies
# ------------------------------
# Install with:  pip install -r requirements.txt
#
# Note: bchlib is a HARD dependency.  crypto_primitives/puf.py
# raises RuntimeError at runtime if bchlib is missing — there is
# no silent fallback.
#
# Note: Kyber and Dilithium are pure-Python reference implementations
# in this codebase (crypto_primitives/kyber.py and dilithium.py).
# They are NOT liboqs-backed.  Absolute timings in the benchmark
# therefore will not match the C-speed numbers reported in Ref [35]'s
# paper and MUST NOT be directly compared to them.

numpy>=1.24
cryptography>=41.0
matplotlib>=3.7
bchlib>=1.0
